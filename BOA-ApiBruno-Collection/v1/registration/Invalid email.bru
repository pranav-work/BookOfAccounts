meta {
  name: Invalid email
  type: http
  seq: 2
}

post {
  url: {{domain}}/api/v1/registration/
  body: json
  auth: inherit
}

body:json {
  {
    "password":"StrongPass123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
