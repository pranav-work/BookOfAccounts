meta {
  name: Logout
  type: http
  seq: 5
}

post {
  url: {{domain}}/api/v1/logout/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

body:json {
  {
    "refresh": "{{refresh_token}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
